@model IEnumerable<Teams>

@{
    ViewData["Title"] = "Teams";
}


<h1>Teams</h1>

@if(Model.Count() > 0) 
{
    <table>
        <thead>
            <tr>
                <th>
                    Teams name
                </th>
                <th>
                    Display order
                </th>
                <th>

                </th>
            </tr>
        </thead>
        <tbody>
            @foreach(var i in Model)
            {
                <tr>
                    <td>@i.Name</td>
                    <td>@i.DisplayOrder</td>
                    <td></td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p> No teams exists. </p>
}
